version: '2.3'

services:

  gym_mupen64plus:
    image: gym_mupen64plus
    container_name: gym_mupen64plus
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - DISPLAY=:0
    volumes:
      - ./:/src
    network_mode: "host"
    command: python example.py
